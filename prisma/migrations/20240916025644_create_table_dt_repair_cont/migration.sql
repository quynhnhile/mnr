-- CreateTable
CREATE TABLE "dt_repair_cont" (
    "id" BIGSERIAL NOT NULL,
    "id_cont" VARCHAR(50) NOT NULL,
    "container_no" VARCHAR(50) NOT NULL,
    "operation_code" VARCHAR(50) NOT NULL,
    "pin_code" VARCHAR(25),
    "order_no" VARCHAR(50),
    "booking_no" VARCHAR(100),
    "bl_no" VARCHAR(100),
    "location" VARCHAR(100),
    "local_size_type" VARCHAR(10) NOT NULL,
    "iso_size_type" VARCHAR(10) NOT NULL,
    "condition_code" VARCHAR(50),
    "classify_code" VARCHAR(50),
    "condition_machine_code" VARCHAR(50),
    "condition_code_after" VARCHAR(50),
    "condition_machine_code_after" VARCHAR(50),
    "factory_date" TIMESTAMPTZ(3),
    "status_code" VARCHAR(5) NOT NULL,
    "survey_in_no" VARCHAR(50),
    "survey_out_not" VARCHAR(50),
    "estimate_no" VARCHAR(50),
    "is_complete" BOOLEAN DEFAULT false,
    "complete_date" TIMESTAMPTZ(3),
    "complete_by" VARCHAR(36),
    "bill_check" BOOLEAN DEFAULT false,
    "bill_date" TIMESTAMPTZ(3),
    "bill_opr_confirm" BOOLEAN DEFAULT false,
    "bill_opr_reason" TEXT,
    "is_posted" BOOLEAN DEFAULT false,
    "note" TEXT,
    "created_date" TIMESTAMPTZ(3) DEFAULT CURRENT_TIMESTAMP,
    "created_by" VARCHAR(36) NOT NULL,
    "modified_date" TIMESTAMPTZ(3),
    "modified_by" VARCHAR(36),

    CONSTRAINT "dt_repair_cont_pkey" PRIMARY KEY ("id")
);
